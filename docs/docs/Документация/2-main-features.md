---
sidebar_position: 2
---

# 2. Основные функции

## 2.1 Вход

Пользователь должен иметь возможность зарегистрироваться и войти в систему.  
Авторизация реализуется через сторонние OAuth-провайдеры.

## 2.2 Управление целями

Пользователь может создавать **цель**.  
Пользователь может создавать промежуточные шаги у **цели**.

Пользователь может редактировать или отменять цель.  
Отменённая цель считается закрытой с вычетом звёзд.

## 2.3 Активация цели

Пользователь может запустить цель в работу, активировав её.
Активировать можно только свою цель, если срок самой цели ещё не прошёл и в ней нет просроченных незавершённых шагов.
Цель можно активировать, даже если у неё пока нет шагов.

После активации цель переходит в статус `active`.
После этого пользователь больше не может добавлять новые шаги, удалять существующие шаги и менять их дедлайны.
Если после активации шаги будут просрочены, они будут учитываться при расчёте штрафа для цели.

## 2.4 Обработка целей

За выполнение цели пользователь получает звёзды.  
За отмену цели — штраф: списание звёзд.
Если звезд 0, то тогда баланс звезд уходит в минус.

## 2.5 Завершение цели

Пользователь может завершить активную цель, если ее дедлайн еще не просрочен и все шаги либо завершены, либо уже просрочены.
Пользователь может добавить комментарий о результате при завершении цели.
При завершении система рассчитывает долю просроченных шагов и определяет, можно ли назначать награду на эту цель.

## 2.6 Просмотр и завершение шагов

Пользователь может просматривать шаги на цели.
На целях в статусе `created` он может добавлять шаги, удалять шаги и редактировать дедлайны.
На целях в статусе `active` он может только просматривать и завершать шаги.

## 2.7 Просмотр целей

Пользователь может просматривать список целей других пользователей.

В списке отображаются:

- Статистика по количеству достигнутых целей и счет звёзд
- Возможность создать **награду** для завершенной цели, если не превышен лимит по просроченным шагам
- Возможность создать большую награду для другого пользователя, если у него есть нужное кол-во звезд

## 2.7 Создание награды на цели

Пользователь может назначить награду на завершённую цель другого пользователя.

Для этого пользователь:

- Переходит в профиль другого пользователя
- Открывает список его целей
- Выбирает завершённую цель, на которую хочет назначить награду
- Нажимает на иконку подарка
- Указывает, какую награду готов подарить
- Подтверждает создание награды

После создания награды:

- Награда привязывается к конкретной цели
- Владелец цели может увидеть, что на его цель назначена награда
- В награде отображается имя пользователя, который её назначил

Дополнительно фиксируются следующие правила:

- Награду можно назначить только на завершённую цель
- Награду нельзя назначить на собственную цель
- На одну цель награды могут назначать неограниченно многие пользователи
- Один и тот же пользователь может назначить на конкретную цель только одну награду

## 2.9 Статистика и награды

За выполнение каждой цели пользователь получает звёзды (например, +10 звёзд).

В профиле отображается:

- Общее число достигнутых целей
- Общее число полученных звёзд
