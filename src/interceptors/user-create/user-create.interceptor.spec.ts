import { UserCreateInterceptor } from './user-create.interceptor';

describe('UserCreateInterceptor', () => {
  it('should be defined', () => {
    expect(new UserCreateInterceptor()).toBeDefined();
  });
});
